<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>Label</key>
    <string>com.jao.sync-trigger</string>

    <key>ProgramArguments</key>
    <array>
        <string>/bin/bash</string>
        <string>__REPO_DIR__/tools/sync-agent/trigger-handler.sh</string>
    </array>

    <!--
      Usamos StartInterval (polling a cada 2s) ao invés de WatchPaths.
      Razão: arquivos criados pelo mount do Cowork (sandbox do Claude) não
      geram FSEvents do kernel macOS, então WatchPaths nunca dispararia
      pra triggers vindos do chat. Polling resolve isso e custa quase nada
      (handler sai em ~5ms quando não há trigger).
      WatchPaths fica adicionalmente como atalho pra triggers criados
      manualmente do Terminal — esses sim geram FSEvents e disparam imediato.
    -->
    <key>StartInterval</key>
    <integer>2</integer>

    <key>WatchPaths</key>
    <array>
        <string>__REPO_DIR__/.sync-trigger</string>
        <string>__REPO_DIR__/.pull-trigger</string>
    </array>

    <key>EnvironmentVariables</key>
    <dict>
        <key>REPO_DIR</key>
        <string>__REPO_DIR__</string>
        <key>HOME</key>
        <string>__HOME__</string>
        <key>PATH</key>
        <string>/usr/local/bin:/opt/homebrew/bin:/usr/bin:/bin:/usr/sbin:/sbin</string>
    </dict>

    <key>RunAtLoad</key>
    <false/>

    <key>KeepAlive</key>
    <false/>

    <key>ThrottleInterval</key>
    <integer>2</integer>

    <key>StandardOutPath</key>
    <string>__HOME__/Library/Logs/jao-sync-agent.stdout.log</string>

    <key>StandardErrorPath</key>
    <string>__HOME__/Library/Logs/jao-sync-agent.stderr.log</string>
</dict>
</plist>
